{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "default colcon build",
            "type": "shell",
            "command": "cd ${workspaceFolder} && colcon build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "default run",
            "type": "shell",
            "command": "./build/rxros/rxros_simple",
            "group": "none",
            "problemMatcher": []
        }
    ]
}